# ProjectTabi 补丁记录

## 0.1.1 — 相机画质与长焦调用（待发布）

- 问题：vivo X100 / OriginOS 6 在较高倍率下，应用拍摄画质与系统相机差距明显；需要保留实时参考图构图。
- 升级 Android CameraX 到 1.6.2，接入系统公开的自动增强、HDR 和夜景模式；按当前镜头检测支持情况，增强启动失败时恢复标准拍摄。
- 修正无逻辑相机时长焦永远无法被识别的问题；按焦距和传感器尺寸识别长焦，同时检测逻辑相机公开的物理镜头。切换长焦后，倍率按该镜头与默认后置镜头的视角关系估算。
- 保留叠图、上下对照和参考图比例；由 CameraX 处理裁剪及方向，移除应用额外的 JPEG 解码与 95 质量重编码，拍摄使用最高 JPEG 质量和可用高分辨率。
- 拍摄页新增“画质”入口，展示实际增强模式、镜头、拍摄分辨率，并支持复制相机信息，便于实机反馈。
- 修正桌面依赖锁文件和 iOS 测试目标残留的上游版本号，统一更新版本元数据。
- 厂商增强和独立长焦的可用性取决于手机固件公开的能力；这些改动不代表已获得 vivo 系统相机的全部 AI 算法。X100 实拍效果待设备验证。

## 0.1.0 — 初始独立版本

- 从 MiriaGo WebSync 拆分为独立的 ProjectTabi，确立新的 Android 与 Windows 应用身份。
- 添加 Windows NSIS 安装包和 Android APK 的 Release 流程，后续版本通过安装包更新。
- 简化 WebDAV 页面，只提供云端连接设置；每次同步前选择“本地 → 云端”或“云端 → 本地”，并显示两端更新时间。
- 记录每次同步的起止时间、方向、结果和图片传输摘要；失败信息包含阶段、原因、处理建议和错误代码。
- 保留 MiriaGo 的 `miriago-webdav` v1 云端清单、`MiriaGoSync` 默认目录、JSON 计划文件及 `.sjhplan` v2 数据包，便于迁移既有记录。
- 延续 WebSync 中的 Android 图片路径读取修复；参考图按内容哈希识别和去重，避免缓存路径导致重复传输。

后续每次 Release 在本文件新增版本条目，记录遇到的问题及对应修复。
