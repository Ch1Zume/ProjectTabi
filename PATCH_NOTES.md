# ProjectTabi 补丁记录

## 0.1.3 — 应用图标与项目介绍（2026-09-25）

- 更换 Windows、Android、iOS、网页及“关于 ProjectTabi”页面的应用图标。
- 整理仓库首页介绍，突出应用功能、导入和同步；将项目来源说明移至末尾的致谢与许可部分。
- 版本升为 0.1.3+5，可通过应用内更新安装。

## 0.1.2 — 拍摄稳定性与设置整理（2026-09-25）

- 问题：切换横竖屏、前后镜头或上下／叠影参考模式后可能黑屏，快门无响应。复用原生预览视图，旧视图仅释放自己绑定的相机用例；重新连接时忽略旧连接的异步结果，减少比例和方向变化引起的重复绑定。
- 相机操作增加超时处理与可读提示，初始化或连接失败时可在拍摄页重新连接；拍摄超时后尝试恢复预览。
- 将自动增强、HDR、夜景、标准模式及“复制相机信息”移至“设置 → 拍摄设置”。画质选择保存在当前设备，下次进入拍摄时应用；诊断信息标明来自上次拍摄。
- 拍摄时不再弹出定位方案设置。旧版尚未选择定位方式的设备默认不写入定位，可在拍摄设置中选择。
- Windows 移除计划、地图、点位详情和导航到达页的相机入口，并隐藏拍摄设置；继续支持查看和同步巡礼记录。
- 相册导入的照片标记为导入来源，保存记录时跳过原照片的系统相册备份；应用内新拍照片继续遵循照片备份开关，对比图遵循独立的自动保存开关。
- 对应 updates.txt 的电脑相机入口和相册重复备份两项问题均已处理。保留 MiriaGo 数据格式和现有更新签名，版本升为 0.1.2+4。
- 相机修复需在 vivo X100 / OriginOS 6 上确认实际表现；构建检查不能代替设备实拍。

## 0.1.1 — 相机画质与应用内更新（2026-09-25）

- 构建 3 补充应用内更新：打开应用后每日自动检查，设置中可查看版本、更新内容与大小，一键下载、取消、重试和安装；提供可选的仅 Wi-Fi 自动下载。
- Android 使用系统下载服务，在后台或应用重启后恢复下载状态；安装前校验文件哈希、包名、版本与现有签名，并引导授予系统安装权限。
- Windows 接入 Tauri 签名更新，下载和验签完成后由用户选择安装时机；安装前检查记录写入或同步状态。
- 版本显示保留 0.1.1，内部构建号升为 3；支持同一版本的后续构建比较。旧构建 2 仍可手动覆盖升级到本次构建，后续通过应用内更新。
- 发布流程生成 latest.json 和 Windows 更新签名，保留 GitHub 手动下载安装入口。更新签名私钥只保存在本机备份与 GitHub Secrets。
- 问题：vivo X100 / OriginOS 6 在较高倍率下，应用拍摄画质与系统相机差距明显；需要保留实时参考图构图。
- 升级 Android CameraX 到 1.6.2，接入系统公开的自动增强、HDR 和夜景模式；按当前镜头检测支持情况，增强启动失败时恢复标准拍摄。
- 修正无逻辑相机时长焦永远无法被识别的问题；按焦距和传感器尺寸识别长焦，同时检测逻辑相机公开的物理镜头。切换长焦后，倍率按该镜头与默认后置镜头的视角关系估算。
- 保留叠图、上下对照和参考图比例；由 CameraX 处理裁剪及方向，移除应用额外的 JPEG 解码与 95 质量重编码，拍摄使用最高 JPEG 质量和可用高分辨率。
- 拍摄页新增“画质”入口，展示实际增强模式、镜头、拍摄分辨率，并支持复制相机信息，便于实机反馈。
- 修正桌面依赖锁文件和 iOS 测试目标残留的上游版本号，统一更新版本元数据。
- 厂商增强和独立长焦的可用性取决于手机固件公开的能力；这些改动不代表已获得 vivo 系统相机的全部 AI 算法。X100 实拍效果待设备验证。

## 0.1.0 — 初始独立版本

- 从 MiriaGo WebSync 拆分为独立的 ProjectTabi，确立新的 Android 与 Windows 应用身份。
- 添加 Windows NSIS 安装包和 Android APK 的 Release 流程，后续版本通过安装包更新。
- 简化 WebDAV 页面，只提供云端连接设置；每次同步前选择“本地 → 云端”或“云端 → 本地”，并显示两端更新时间。
- 记录每次同步的起止时间、方向、结果和图片传输摘要；失败信息包含阶段、原因、处理建议和错误代码。
- 保留 MiriaGo 的 `miriago-webdav` v1 云端清单、`MiriaGoSync` 默认目录、JSON 计划文件及 `.sjhplan` v2 数据包，便于迁移既有记录。
- 延续 WebSync 中的 Android 图片路径读取修复；参考图按内容哈希识别和去重，避免缓存路径导致重复传输。

后续每次 Release 在本文件新增版本条目，记录遇到的问题及对应修复。
