package com.ch1zume.projecttabi

import androidx.core.content.FileProvider

/** Dedicated provider for the APK download directory, as recommended by AndroidX. */
class UpdateFileProvider : FileProvider()
